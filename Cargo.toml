# Workspace containing all RPFM crates.
[workspace]
members = [
    # Libs
    "rpfm_lib",
    "rpfm_extensions",

    # Executables
    "rpfm_cli",
    # "rpfm_mod_checker",
    #"rpfm_ui",
]

[profile.dev]
incremental = true

[profile.release]
lto = true
debug = true
