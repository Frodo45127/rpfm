# Workspace containing all RPFM crates.
[workspace]
members = [
    "rpfm_macros",
    "rpfm_mod_checker",
    "rpfm_logging",
    "rpfm_packedfiles",
    "rpfm_common",
]

[profile.dev]
incremental = true

[profile.release]
lto = true
debug = true
