# Workspace containing all RPFM crates.
[workspace]
members = [
    "rpfm_macros",
    "rpfm_logging",
    "rpfm_common",
    "rpfm_files",
]

[profile.dev]
incremental = true

[profile.release]
lto = true
debug = true
